# Tracking SDK iOS 5.3.1

## 1. 更新事件 147（Update Event 147）

用于在登录成功后立即检查并显示「Event Profile」（活动信息）画面。

应在 `SdkDelegate` 的 `loginSuccess` 回调中立即调用，且在进入游戏画面之前调用。

```objectivec
[[Sdk sharedInstance] showOrHide147];
```

所有埋点相关函数均通过以下单例调用：

```objectivec
[[SdkTracking sharedInstance] ...];
```

所需 Framework：`ISdkTrackier.xcframework`（已包含在 SDK 集成所需文件清单中）。

## 2. 绑定用户信息（Mapping User）

用于将当前角色/服务器信息绑定到埋点系统。建议在玩家选择服务器或选择角色后立即调用，并且要在调用其他与该角色相关的日志函数之前调用。

```objectivec
-(void) mappingUser:(NSString*)characterName
characterId:(NSString*)characterId
characterLevel:(NSString*)characterLevel
serverName:(NSString*)serverName
serverId:(NSString*)serverId;
```

参数：

| 参数 | 说明 |
|---|---|
| characterName | 角色名称 |
| characterId | 角色 ID |
| characterLevel | 角色等级 |
| serverName | 服务器名称 |
| serverId | 服务器 ID |

示例：

```objectivec
[[SdkTracking sharedInstance] mappingUser:@"name"
characterId:@1
characterLevel:@1
serverName:@"test"
serverId:@"1"];
```

## 3. 日志上报函数（Log Function）

这些日志函数用于跟踪用户的使用路径：打开游戏 → 下载/解压资源 → 登录/选择服务器 → 创建或选择角色 → 进入游戏 → 新手引导。

依据原始流程图的颜色说明：

- **灰色**：游戏内部发生的步骤。
- **黄色**：游戏调用 SDK 的节点（即下方的日志函数）。

总体声明（无参数）：

```objectivec
-(void) logStartGameScreen;
-(void) logEnterGamePlay;
-(void) logStartTutorial;
-(void) logCompleteTutorial;
```

### 3.1. logStartGameScreen

在打开「开始游戏」画面（进入游戏前的起始画面）时调用。

```objectivec
[[SdkTracking sharedInstance] logStartGameScreen];
```

### 3.2. logEnterGamePlay

跟踪用户在点击「进入游戏」按钮后进入游戏画面（Game Play Screen）的行为。

```objectivec
[[SdkTracking sharedInstance] logEnterGamePlay];
```

应在处理「进入游戏」按钮点击事件的位置立即调用，且在切换到游戏画面之前调用。

### 3.3. 资源下载（Resource download）

#### 开始下载资源

```objectivec
// 开始下载资源
-(void) logPatchStart:(NSNumber *_Nullable)sizeMB
withVerrsion:(NSString *_Nullable)version
withNetworkType:(NSString *_Nullable)networkType;
```

| 参数 | 说明 |
|---|---|
| sizeMB | 需要更新的补丁文件大小（以 Mb 显示） |
| version | 安装包版本号 |
| networkType | 网络类型（例如：`"wifi"`） |

```objectivec
[[SdkTracking sharedInstance] logPatchStart:@"500"
withVerrsion:@"1.0.1"
withNetworkType:@"wifi"];
```

#### 资源下载成功

```objectivec
-(void) logPatchSuccess:(NSNumber *_Nullable)durationSec
withAvgSpeedKbps:(NSNumber *_Nullable)avgSpeedKbps
withCdnNode:(NSString *_Nullable)cdnNode;
```

| 参数 | 说明 |
|---|---|
| durationSec | 从开始下载到下载完成所用的时间 |
| avgSpeedKbps | 平均下载速度（若 `update_type` 为 Store，请再确认能否取得该值） |
| cdnNode | 本次下载所使用的 CDN 节点 |

```objectivec
[[SdkTracking sharedInstance] logPatchSuccess:@"500"
withAvgSpeedKbps:@"450"
withCdnNode:@"192.168.0.2"];
```

> **注意：** 原文档示例中重复出现了 `avgSpeedKbps` 参数（原文档的笔误）；上方示例已按函数签名去除重复部分。

#### 资源下载失败

```objectivec
// 当资源下载中断或失败时
-(void) logPatchError:(NSString *_Nullable)errorCode
withRetryCount:(NSNumber *_Nullable)retryCount
withFailedUrl:(NSString *_Nullable)failedUrl
withCdnNode:(NSString *_Nullable)cdnNode;
```

| 参数 | 说明 |
|---|---|
| errorCode | 错误代码 |
| retryCount | 因该错误需要重试的次数 |
| failedUrl | 出错的 URL |
| cdnNode | 出错的 CDN 节点 |

```objectivec
[[SdkTracking sharedInstance] logPatchError:@"500"
withRetryCount:@1
withFailedUrl:@"https://cdn.game.com/patch/v120.zip"
withCdnNode:@"192.168.0.2"];
```

### 3.4. 资源解压（Resource extraction）

#### 开始解压

下载完成后立即开始解压/安装：

```objectivec
-(void) logExtractStarted:(NSString *_Nullable)totalSizeMb
withTotalSize:(NSString *_Nullable)totalSize;
```

| 参数 | 说明 |
|---|---|
| totalSizeMb | 解压后的大小（以 Mb 计） |
| totalSize | 补丁中需要解压的文件数量（若无法获取则忽略） |

```objectivec
[[SdkTracking sharedInstance] logExtractStarted:@"500"
withTotalSize:@"400"];
```

#### 解压完成

解压完成后，准备进入登录画面：

```objectivec
-(void) logExtractFinished:(NSNumber *_Nullable)durationSec
withResultCode:(NSString *_Nullable)resultCode;
```

| 参数 | 说明 |
|---|---|
| durationSec | 从开始解压到解压成功所用的时间 |
| resultCode | 结果代码（`0`/`"ok"`：成功，`1`：磁盘已满） |

```objectivec
[[SdkTracking sharedInstance] logExtractFinished:@100
withResultCode:@"ok"];
```

### 3.5. 创建角色（Character creation）

#### 开始创建角色

```objectivec
-(void) logCharCreateStart:(NSString *_Nullable)serverId;
```

| 参数 | 说明 |
|---|---|
| serverId | 服务器 ID |

```objectivec
[[SdkTracking sharedInstance] logCharCreateStart:@"100"];
```

> **注意：** 需要使用计时器来计算从开始创建角色到创建完成所用的时间（`durationSec` 用于 `logCharCreateSuccess`）。

#### 角色创建成功

```objectivec
-(void) logCharCreateSuccess:(NSString *_Nullable)serverId
withCharId:(NSString *_Nullable)charId
withCharName:(NSString *_Nullable)charName
withCharClass:(NSString *_Nullable)charClass
withDurationSec:(NSNumber *_Nullable)durationSec;
```

| 参数 | 说明 |
|---|---|
| serverId | 服务器 ID |
| charId | 角色 ID |
| charName | 角色名称 |
| charClass | 角色职业/Class ID |
| durationSec | 从开始创建角色到创建成功所用的时间 |

```objectivec
[[SdkTracking sharedInstance] logCharCreateSuccess:@"100"
withCharId:@"1001"
withCharName:@"test"
withCharClass:@"1"
withDurationSec:@5];
```

> **注意：** 原文档示例错误地照搬了 `logPatchStart` 函数的参数（`withVerrsion`、`withNetworkType`），与 `logCharCreateSuccess` 的签名不符。上方示例已按该函数的实际参数重新编写；如需 100% 核对，建议与 SDK 团队（VTVLive）确认。

#### 角色创建失败

```objectivec
-(void) logCharCreateError:(NSString *_Nullable)errorCode
withCharName:(NSString *_Nullable)charName
withServerId:(NSString *_Nullable)serverId;
```

| 参数 | 说明 |
|---|---|
| errorCode | 错误代码（例如：`"NAME_DUPLICATED"`） |
| charName | 角色名称 |
| serverId | 服务器 ID |

```objectivec
[[SdkTracking sharedInstance] logCharCreateError:@"NAME_DUPLICATED"
withCharName:@"test"
withServerId:@"1"];
```

### 3.6. 选择角色（Character select）

选择服务器/选择角色完成后，调用此函数：

```objectivec
-(void) logCharSelect:(NSString *_Nullable)charId
withCharacterName:(NSString *_Nonnull)characterName
withCharLevel:(NSString *_Nullable)charLevel;
```

| 参数 | 说明 |
|---|---|
| charId | 角色 ID |
| characterName | 角色名称 |
| charLevel | 角色等级 |

```objectivec
[[SdkTracking sharedInstance] logCharSelect:@"1"
withCharacterName:@"name"
withCharLevel:@"1"];
```

### 3.7. 新手引导（Tutorial）

两个无参数的声明函数（用于跟踪游戏内新手引导的开始/完成）：

```objectivec
[[SdkTracking sharedInstance] logStartTutorial];
[[SdkTracking sharedInstance] logCompleteTutorial];
```

## 4. 埋点流程图（Flowchart）

该流程图重新绘制了 `SdkTracking` 各函数的完整调用流程，参照原文档中的流程图，并按照游戏流程分为 3 个部分。

颜色说明：

- **灰色方框**：游戏内部发生的步骤。
- **蓝色（菱形）**：分支/判断节点。
- **黄色方框**：游戏调用 SDK 的节点（即 `SdkTracking` 相关函数）。

### 4.1. 资源下载与解压

![资源下载与解压流程](/images/sdkgame/ios/objective_c/tracking_guide/resource-download-extraction-flow-zh.png)

*图 1 — 资源下载与解压流程*

### 4.2. 登录、创建/选择角色

![登录、创建或选择角色流程](/images/sdkgame/ios/objective_c/tracking_guide/login-character-flow-zh.png)

*图 2 — 登录、创建或选择角色流程*

### 4.3. 进入游戏与新手引导

![进入游戏与新手引导流程](/images/sdkgame/ios/objective_c/tracking_guide/enter-game-tutorial-flow-zh.png)

*图 3 — 进入游戏与新手引导流程*

> **注意：** 该流程图根据原文档流程图中的函数名称/步骤顺序重新绘制，力求清晰易读 — 并未直接照搬原图中较小的示例参数值（原图中部分参数标注与函数签名不符，详见上方第 3.3 节与第 3.5 节的说明）。

## 5. 按游戏流程调用顺序汇总

1. 打开游戏 → 是否需要下载更新？→ `logPatchStart` → 下载完成：`logPatchSuccess`，或失败：`logPatchError`
2. 开始解压：`logExtractStarted` → 解压完成：`logExtractFinished`
3. 打开起始画面：`logStartGameScreen`
4. 登录/选择服务器成功
5. 创建新角色：`logCharCreateStart` → 成功：`logCharCreateSuccess`/失败：`logCharCreateError`（或）选择已有角色：`logCharSelect`
6. 将角色信息绑定到埋点系统：`mappingUser`
7. 点击「进入游戏」：`logEnterGamePlay`
8. 在新手引导流程中：`logStartTutorial`/`logCompleteTutorial`

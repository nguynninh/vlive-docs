# Update SDK 5.1.4

Change history:

- Update AppsFlyerFramework 6.17.7

- Update Receive Gift

- Update Switch account

1. Profile

```SQL
platform :ios, '15.0'
target 'SdkExample' do
  use_frameworks!
  pod 'GoogleSignIn', '7.1.0'
  pod 'Firebase', '12.0.0'
  pod 'FirebaseMessaging', '12.0.0'
  pod 'FirebaseRemoteConfig', '12.0.0'
  pod 'AppsFlyerFramework', '6.17.7'
  pod 'JWT', '2.2.0'
  pod 'TikTokBusinessSDK', '1.5.0'
  pod 'FirebaseRemoteConfig', '12.0.0'
  pod 'FirebaseCrashlytics', '12.0.0'
end
target 'FirebaseNotificationServiceExtension' do
 use_frameworks!
 pod 'Firebase', '12.0.0'
 pod 'FirebaseMessaging', '12.0.0'

end
post_install do |lib|
    lib.pods_project.targets.each do |target|
        if target.name == 'GoogleSignIn' || target.name == 'Firebase' || target.name == 'FirebaseCrashlytics'|| target.name == 'FirebaseRemoteConfig' || target.name == 'FirebaseMessaging' || target.name == 'AppsFlyerFramework' ||  target.name == 'JWT' ||  target.name == 'TikTokBusinessSDK'
            target.build_configurations.each do |config|
                config.build_settings['BUILD_LIBRARY_FOR_DISTRIBUTION'] = 'YES'
            end
        end
        
        target.build_configurations.each do |config|
            config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
        end
    end
end
```

Run pod:

`pod cache clean --all`

`pod deintegrate`

`pod install`

2. Update AppDelegate

```Plain Text

@import Firebase;



- (BOOL)application:(UIApplication *)application didFinishLaunchingWithOptions:(NSDictionary *)launchOptions {
    [[Sdk sharedInstance] initWithDelegate:self application:application launchOptions:launchOptions isProdEnv:TRUE];
    [[Sdk sharedInstance] setLocationFloatingButton:20 with:100];
    [[Sdk sharedInstance] setGrantEnv:NO];
    return YES;
}

-(void) getLangaugeSdk: (NSString *) langauge {
    // print  Langauge
}

- (void)application:(UIApplication *)application didRegisterForRemoteNotificationsWithDeviceToken:(NSData *)deviceToken {
    [FIRMessaging messaging].APNSToken = deviceToken;
}

- (void)messaging:(FIRMessaging *)messaging didReceiveRegistrationToken:(NSString *)fcmToken {
}
```

![Screenshot 2026-04-21 at 11.27.51.png](/images/sdkgame/ios/objective_c/update_sdk_5_1_4/Screenshot-2026-04-21-at-11.27.51.png)

3. Receive Gift

(You need to initialize characterId and serverId before calling showReceiveGift)

When the user clicks your gift box button, please call this function:
`[[Sdk sharedInstance] showReceiveGift];`

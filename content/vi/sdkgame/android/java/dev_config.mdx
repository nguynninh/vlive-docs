# Hướng dẫn cấu hình game dành cho developer

## Nhận thông tin cấu hình game

1. Nhận thông tin cấu hình từ email

![image.png](/images/sdkgame/android/java/dev_config/image-4.png)

2. Nhận thông tin đã cấu hình trên hub từ backend

![image.png](/images/sdkgame/android/java/dev_config/image-3.png)

3. Nhận thông tin toàn bộ cấu hình từ trưởng phòng

![image.png](/images/sdkgame/android/java/dev_config/image-2.png)

## Quy trình cấu hình

1. Cấu hình trên Hub

    Sau khi cấu hình trên hub thành công và kiểm tra bằng cách copy data này vào sdk để kiểm tra các trường có đúng không thì hãy cấu hình nó lên firebase remote config giống như các game khác.

![image.png](/images/sdkgame/android/java/dev_config/image-8.png)

    1. Global

        Áp dụng cho tất cả các ứng dụng được tạo tự động không cần sửa trừ khi sai số hãy báo cho bên BE sửa. Giá trị mặc định là 0

    2. App

        Áp dụng cho tất cả các package kể cả iOS, tham số lấy ở appId

![image.png](/images/sdkgame/android/java/dev_config/image-6.png)

    1. Package

        Áp dụng cho package duy nhất lấy ở packageId

![image.png](/images/sdkgame/android/java/dev_config/image-10.png)

    2. Tham số cấu hình danh sách

| STT | Tên trường | Copy từ dự án cũ | Kiểu dữ liệu | Yêu cầu tham số | Mô tả trường |
| --- | --- | --- | --- | --- | --- |
| 1 | hub_api_base_url | Có thể | String | Bắt buộc | Base Url Hub |
| 2 | authen_api_base_url | Có thể | String | Bắt buộc | Base Url Auth |
| 3 | payment_api_base_url | Có thể | String | Bắt buộc | Base Url Payment |
| 4 | translate_api_base_url | Có thể | String | Bắt buộc | Base Url Translate |
| 5 | client_id | Không thể | String | Bắt buộc | Client Id của ứng dụng cấp từ BE trong email |
| 6 | client_secret | Không thể | String | Bắt buộc | Client Secret của ứng dụng cấp từ BE trong email |
| 7 | google_web_client | Không thể | String | Bắt buộc | Google web client id (không phải android client id) được lấy từ google cloud, cần liên kết đúng với sha1 của keystore mới có thể đăng nhập google |
| 8 | achieved_levels | Có thể | Array | Bắt buộc | Giá trị để tracking level của user |
| 9 | achieved_vips | Có thể | Array | Bắt buộc | Giá trị để tracking vip của user |
| 10 | af_dev_key | Có thể | String | Bắt buộc | Key của appsflyer |
| 11 | scope | Có thể | String | Bắt buộc | Quyền truy cập của api |
| 12 | app_id | Không thể | String | Bắt buộc | appId, giá trị này tương đương app ở trên |
| 13 | apple_client_id | Không thể | String | Không bắt buộc | Giá trị này sử dụng để đăng nhập apple (hiện đã bị loại bỏ, điền cho đỡ thừa) |
| 14 | api_key_recapcha | Có thể | String | Bắt buộc | Key của recapcha v3 |
| 15 | package_id | Không thể | String | Bắt buộc | Package Id của ứng dụng |
| 16 | tik_tok_app_id | Không thể | String | Không bắt buộc | Tiktok app id, sau khi app lên store và có thể tải thì mới cấu hình được cái này |
| 17 | access_token_tik_tok | Không thể | String | Không bắt buộc | Tiktok accesstoken, sau khi app lên store và có thể tải thì mới cấu hình được cái này |
| 18 | multilingual | Không thể | String | Bắt buộc | Ẩn hiện chọn ngôn ngữ trên giao diện login. 1 là hiện, còn lại là ẩn |
| 19 | language_list | Không thể | Array | Bắt buộc | Mảng dữ liệu dành cho danh sách đa ngôn ngữ, mặc định để 1 ngôn ngữ duy nhất. Ngôn ngữ đầu tiên sẽ là ngôn ngữ mặc định |
| 20 | app_code | Không thể | String | Bắt buộc | Tên app của ứng dụng, ví dụ: R001, T026, T028 |
| 21 | event_countdown | Không thể | Json | Không bắt buộc | Là Json chứa thông tin đếm ngược ngày phát hành dành cho ứng dụng sắp phát hành |
| 22 | version_store | Không thể | String | Bắt buộc | Là thông tin phiên bản mới sau khi gửi review thành công và đã phát hành trên store. Cần cập nhật khi phát hành phiên bản mới nhất |
| 23 | show_floating | Có thể | String | Bắt buộc | Ẩn hiện button floating khi hiển thị sau khi đăng nhập: 0 là ẩn, 1 là hiển thị |
| 24 | hidden_floating_version | Không thể | String | Bắt buộc | Đây là phiên bản ẩn button floating khi gửi review trên store, sau khi review xong thì phải xoá đi để rỗng |
| 25 | reward_profile_event | Có thể | Json | Bắt buộc | Phải thay ảnh trước khi lên prod và phát hành app, tính năng này phải đi kèm với tính năng nhận quà trong game |

    3. Cấu hình sự kiện bắn từ server lên firebase

        Yêu cầu trưởng phòng cập nhật thông tin hai tham số này sự kiện mới có thể bắn lên firebase

![image.png](/images/sdkgame/android/java/dev_config/image-5.png)

    4. Cấu hình gói nạp theo danh sách gọi nạp được gửi. Cái này cần cẩn thận không là đền tiền đấy

2. Cấu hình trên firebase

    1. Cấu hình ứng dụng

        Truy cập: Setting -> General -> Add app -> Điền package Id

        Sau khi tạo app **Đấm Trưởng Phòng** cập nhật cấu hình trên google cloud để bổ sung thông tin cho google-service.json

        Kiểm tra json tại: https://dandar3.github.io/android/google-services-json-to-xml.html

        Cần đủ như hình

![image.png](/images/sdkgame/android/java/dev_config/image.png)

    2. Cấu hình thông báo từ server

        Sau khi cấu hình đúng google-server.json vào app mẫu thì chắc chắn sẽ nhận được noti, có thể test trước bằng message của firebase

        Project Setting -> Services account

        Hãy đảm bảo chỉ tạo 1 lần duy nhất và lưu file này lại.

        Khi tạo cấu hình cho cả Android và iOS ở Hub và thử lại thông báo từ cấu hình của package trên hub

![image.png](/images/sdkgame/android/java/dev_config/image-7.png)

        Set ở trên hub

![image.png](/images/sdkgame/android/java/dev_config/image-9.png)

3. Cấu hình trên facebook

    1. Cấu hình app

        Cài đặt ứng dụng -> Thông tin cơ bản -> Thêm nền tảng -> Google play

        Hash chính:

        Lấy SHA1 từ store sau khi store ký để tạo bằng link và dán vào đây https://tomeko.net/online_tools/hex_to_base64.php

        Tên gói: Tên Package

        Tên Lớp: MainActivity

![image.png](/images/sdkgame/android/java/dev_config/image-1.png)

4. Cấu hình example

    Vui lòng tham khảo dự án khác

    - Đổi package name
    - Đổi tên dự án
    - Đổi thông tin ở string.xml
    - Đổi google-service.json
    - Đổi file vtvlive_config_sdk_game_defaults -> thông tin lấy từ hub
    - Đổi key để tải thư viện vào 2 sdk game và game tracking, tạo key accesstoken -> developer -> read-api và lưu lại

5. Quy trình test ví dụ mẫu

    1. Đăng nhập thường

        Thất bại -> Kiểm tra lại cấu hình hub và dự án mẫu xem có đúng package và thông tin được cấp -> Đúng thì báo bên BE xem đã cấu hình đăng nhập chưa

    2. Đăng ký và Quên Mật Khẩu

        Thất bại -> Mở Log nhật ký thấy báo package name không tồn tại ở recaptcha -> **Đấm Trưởng Phòng** cấu hình

        Thất bại -> Hiển thị dialog báo lỗi từ BE -> Thiết bị và mạng bị spam bởi recaptcha, sử dụng thiết bị và mạng 3G khác

    3. Đăng nhập google

        Xác thực thông tin thất bại.

        Nguyên nhân:

        -> Ứng dụng build chưa ký, hoặc chữ ký bị sai ở google cloud. Cần kiểm tra 3 chỗ Apk, google cloud, store phải chung chữ ký sha1

        ! Có thể GG login client ID trên hub đang set sai

    4. Đăng nhập facebook

        Báo lỗi không đăng nhập được.

        Ở chế độ develop:

        Kiểm tra lại thông tin của facebook trong app

        Sử dụng đúng tài khoản thử nghiệm để đăng nhập không

        Ở chế độ public:

        Kiểm tra facebook đã cấp quyền profile chưa -> Chưa có **Đấm Trưởng Phòng** xin quyền từ facebook cho dự án

    5. Kiểm tra sự kiện lên appsflyer

        Mở log đặt tag là appsflyer, nếu bắn lên báo lỗi package name không tồn tại **Đấm Trưởng Phòng**

    6. Kiểm tra thanh toán

        Muốn thanh toán được cần đẩy 1 bản lên thử nghiệm nội bộ sau đó cấu hình gói nạp trên store và hub

        Nếu không thanh toán được báo lỗi không xác định

        Kiểm tra apk đã truyền đúng tên gói nạp chưa.

        Nếu bên game đã truyền đúng tên gói nạp mà api BE trả về lỗi không xác định

        **Đấm Trưởng Phòng** để trưởng phòng báo bên Thanh toán bật trên production và cấu hình bật service trên google cloud.

        Khi bật cấu hình này cần lâu nhất là 1 ngày mới có thể thanh toán được

## Cấu hình store

1. Đăng ký ứng dụng

    Việc đăng ký cần được làm ngay vì cần tới 48h để review đăng ký tên gói.

    Sau khi hoàn thành dự án mẫu, tạo file theo đường dẫn

![store-register.png](/images/sdkgame/android/java/dev_config/store-register.png)

    Mã được lấy từ store khi đăng ký package name

    Sử dụng đúng keystore và build ra apk rồi đưa lên store để đăng ký package name

    Sau khi đăng ký package name thì mới có thể tạo ứng dụng đó trên store
